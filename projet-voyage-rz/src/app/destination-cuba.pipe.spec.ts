import { DestinationCubaPipe } from './destination-cuba.pipe';

describe('DestinationCubaPipe', () => {
  it('create an instance', () => {
    const pipe = new DestinationCubaPipe();
    expect(pipe).toBeTruthy();
  });
});
