import { DestinationMexiquePipe } from './destination-mexique.pipe';

describe('DestinationMexiquePipe', () => {
  it('create an instance', () => {
    const pipe = new DestinationMexiquePipe();
    expect(pipe).toBeTruthy();
  });
});
